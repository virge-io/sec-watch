[project]
name = "sec-watch"
version = "0.1.0"
description = "Local security watcher with a FastAPI repository scan webserver"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.27",
]

[tool.uv]
package = false
